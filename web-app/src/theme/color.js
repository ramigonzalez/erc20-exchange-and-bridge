const black = '#000000';
const white = '#FFFFFF';
const gray = '#808080';

const primary = black;
const secondary = gray;

const color = {
    black,
    gray,
    primary,
    secondary,
    white,
};

export default color;
